# BrewCode OS 白皮书 · 序章

**版本：** v0.1
**日期：** 2026-06-16
**许可：** CC0 (公共领域)

---

## 摘要

BrewCode OS 是一个为全球咖啡冲煮建立通用数字语言的开放项目。其核心资产是一个名为 `.brew` 的 JSON 标准文件格式，以及围绕此标准构建的工具矩阵和 AI 服务层。BrewCode OS 的目标是成为咖啡冲煮领域的 IEEE——定义标准，而非生产商品。

---

## 问题陈述

咖啡冲煮领域面临三个根本问题：

1. **知识碎片化**：冲煮方案散落在社交媒体、视频平台和私人笔记中，缺乏统一的数据格式和检索体系。
2. **参数不可比较**：不同设备之间的研磨刻度、水温基准、注水手法无法直接比较。"Comandante 22 格"与"EK43 刻度 10"之间的对应关系需要人工翻译。
3. **数据主权缺失**：咖啡爱好者的冲煮记录、味觉偏好、设备参数被封闭的商业平台收集和变现，用户对自己的数据没有控制权。

---

## 解决方案

BrewCode OS 提出了一个三层的解决方案：

### 第一层：开源标准

`.brew` JSON Schema 定义了冲煮方案的完整数据结构，包括：

- **meta**：方案元信息（名称、作者、许可证、标签）
- **coffee**：咖啡豆信息（产地、品种、处理法、烘焙度）
- **equipment**：器具信息（滤杯、磨豆机、手冲壶）
- **recipe**：冲煮参数（粉量、水量、粉水比、研磨度、水温、时间）
- **steps**：冲煮步骤列表（每步含操作类型、注水量、时间、注水手法）
- **result**：结果记录（实际 TDS、萃取率、风味评价）

`.brew` 标准以 CC0 协议发布，永久免费、永久开源。

### 第二层：工具矩阵

围绕 `.brew` 标准构建了三个核心工具：

- **BrewPlayer**：`.brew` 文件的运行时环境，将参数转化为分步冲煮引导。
- **BrewRepo**：`.brew` 文件的公共版本库，支持 Fork、PR、Issues 协作。
- **BrewForge**：`.brew` 文件的可视化编辑器和 JSON 编辑器，支持实时 Schema 校验。

三者通过 `.brew` 文件格式互通，各自独立，互不绑定。

### 第三层：AI 服务

基于 `.brew` 标准的结构化数据，训练和部署了三个 AI 服务：

- **风味诊断**：分析用户描述的问题和当前方案参数，输出具体调整建议。
- **方案生成**：根据豆子信息、设备列表和口味偏好，生成完整的冲煮方案。
- **语义翻译**：将不同设备的刻度值翻译为可比较的物理参数（如粒径微米数）。

所有 AI 服务的推理结果基于公共数据和用户显式授权的个人数据，绝不会在未授权的情况下使用用户数据。

---

## 核心原则

### 1. 风味-参数同构

任何可描述的风味差异，必然对应一组可测量的参数差异。咖啡冲煮不是玄学，是可以被编程的物理。

### 2. 知识开源主义

冲煮知识是人类公共遗产。BrewCode OS 的核心标准、协议、参考实现，永远以公共领域或最宽松的开源许可证发布。

### 3. 数据主权

用户拥有其冲煮数据的完全主权。任何数据的使用必须基于用户的显式授权。BrewCode OS 绝不售卖、出租或未经授权分析用户数据。

### 4. 工具隐去

最好的冲煮工具在使用时"抽身而去"，让人直接与咖啡对话。所有界面设计遵循"扫一眼就够"的原则，不做视觉噪音。

---

## 商业模型

BrewCode OS 本身永远免费、开源。运营成本通过以下方式覆盖：

- **AI 增值服务**：风味诊断和方案生成按次付费或月度订阅
- **BrewCode Compatible 认证**：硬件厂商接入标准时支付年费认证
- **匿名聚合数据报告**：基于社区授权数据的行业洞察报告
- **社区自愿赞助**

任何商业行为不得破坏标准的开放性和数据的中立性。

---

## 长期愿景

BrewCode OS 的最终形态是成为咖啡冲煮领域的通用数字基础设施——就像 HTTP 之于万维网，Git 之于版本控制，IEEE 802.11 之于无线通信。

当最后一台咖啡机停止运转，当最后一支咖啡豆被采摘，BrewCode OS 所记录的每一杯咖啡的参数和背后的故事，将成为人类感官文化史的永久见证。

这不是一个产品。这不是一个公司。
这是一粒种子——一个为咖啡冲煮建立通用语言的开源运动。

---

_BrewCode OS 诞生于 2026 年。白皮书将持续演进，欢迎通过 GitHub 提交贡献。_
