# BrewCode OS 白皮书 · 序章

**版本：** v0.2
**日期：** 2026-07-24
**许可：** CC0 (公共领域)

---

## 摘要

BrewCode OS 是一个为全球咖啡冲煮建立通用数字语言的开放项目。其核心资产是一个名为 `.brew` 的 JSON 标准文件格式，以及围绕此标准构建的工具矩阵与 AI 服务层。BrewCode OS 的目标是成为咖啡冲煮领域的通用数字基础设施——定义标准，而非生产商品。

---

## 问题陈述

咖啡冲煮领域面临三个根本问题：

1. **知识碎片化**：冲煮方案散落在社交媒体、视频平台与私人笔记中，缺乏统一的数据格式与检索体系。
2. **参数不可比较**：不同设备之间的研磨刻度、水温基准、注水手法无法直接比较。"Comandante 22 格"与"EK43 刻度 10"之间的对应关系需要人工翻译。
3. **数据主权缺失**：咖啡爱好者的冲煮记录、味觉偏好、设备参数被封闭的商业平台收集与变现，用户对自己的数据没有控制权。

---

## 解决方案

BrewCode OS 提出了一个三层的解决方案：

### 第一层：开源标准

`.brew` JSON Schema 定义了冲煮方案的完整数据结构，包括：

- **meta**：方案元信息（名称、作者、许可证、标签）
- **coffee**：咖啡豆信息（产地、品种、处理法、烘焙度）
- **equipment**：器具信息（滤杯、磨豆机、手冲壶）
- **recipe**：冲煮参数（粉量、水量、粉水比、研磨度、水温、时间）
- **steps**：冲煮步骤列表（每步含操作类型、注水量、时间、注水手法）
- **result**：结果记录（实际 TDS、萃取率、风味评价）

`.brew` 标准以 CC0 协议发布，永久免费、永久开源。

### 第二层：工具矩阵

围绕 `.brew` 标准构建了三个核心工具：

- **BrewPlayer**：`.brew` 文件的运行时环境，将参数转化为分步冲煮引导。
- **BrewRepo**：`.brew` 文件的公共版本库，支持 Fork、PR、Issue 协作。
- **BrewForge**：`.brew` 文件的可视化编辑器与 JSON 编辑器，支持实时 Schema 校验。

三者通过 `.brew` 文件格式互通，各自独立，互不绑定。

### 第三层：AI 服务

基于 `.brew` 标准的结构化数据，部署了三个 AI 服务：

- **风味诊断**：分析用户描述的问题与当前方案参数，输出具体调整建议。
- **方案生成**：根据豆子信息、设备列表与口味偏好，生成完整的冲煮方案。
- **语义翻译**：将不同设备的刻度值翻译为可比较的物理参数（如粒径微米数）。

所有 AI 服务的推理结果基于公共数据与用户显式授权的个人数据，绝不会在未授权的情况下使用用户数据。

---

## 核心原则

### 1. 风味-参数同构

任何可描述的风味差异，必然对应一组可测量的参数差异。咖啡冲煮不是玄学，是可以被编程的物理。

### 2. 知识开源主义

冲煮知识是人类公共遗产。BrewCode OS 的核心标准、协议与参考实现，永远以公共领域或最宽松的开源许可证发布。

### 3. 数据主权

用户拥有其冲煮数据的完全主权。任何数据的使用必须基于用户的显式授权。BrewCode OS 绝不售卖、出租或未经授权分析用户数据。

### 4. 工具隐去

最好的冲煮工具在使用时"抽身而去"，让人直接与咖啡对话。所有界面设计遵循"扫一眼就够"的原则，不做视觉噪音。

---

## `.brew` Schema 规范

`.brew` 是 BrewCode OS 的事实标准载体。其规范遵循"可验证、可演进、可互操作"三原则。

### 数据结构

一份 `.brew` 文件由以下顶层对象构成：

| 字段 | 职责 | 关键约束 |
|------|------|----------|
| `meta` | 方案身份与许可 | 含 `name`、`author`、`license`（默认 CC0）、`tags` |
| `coffee` | 咖啡豆溯源 | `origin`、`variety`、`process`、`roastLevel` |
| `equipment` | 器具声明 | `dripper`、`grinder`、`kettle` 及型号 |
| `recipe` | 冲煮参数 | `dose`、`water`、`ratio`、`grind`、`temperature`、`time` |
| `steps` | 步骤序列 | 有序数组，每步含 `type`、`water`、`duration`、`pour` |
| `result` | 结果记录（可选） | `tds`、`extraction`、`flavor` |

### 版本与兼容性

- Schema 采用语义化版本（`schemaVersion`，如 `1.0.0`）。
- 主版本变更须附带向后不兼容说明；次版本可新增可选字段；修订版本仅修复约束。
- 写入方须声明 `schemaVersion`；读取方须对未知可选字段保持前向兼容。
- 校验以 JSON Schema 实现：BrewForge 提供实时校验，BrewRepo 在 CI 中强制校验。

### 单位与基准

为消解"参数不可比较"问题，`.brew` 约定：温度统一为摄氏度；研磨度在保留设备原生刻度的同时记录估算粒径（微米）；时间统一为秒。语义翻译服务负责设备原生刻度与物理参数的双向映射。

---

## 治理模型

BrewCode OS 不是一家公司，而是一套由社区共同维护的开放标准。其治理目标与"知识治理"一致：让冲煮知识作为人类公共遗产，被开放地记录、验证与传承。

### 标准治理

- `.brew` Schema 的演进通过公开提案（RFC）进行，任何人都可在 GitHub 提交。
- 重大变更须经社区评审与版本投票，决策过程公开、可追溯。
- 规范文档与参考实现永久以 CC0 或最宽松开源许可证发布。

### 数据治理

- **数据主权**：用户拥有其冲煮数据的完全主权，任何使用须基于显式授权。
- **匿名聚合**：社区数据集仅以去标识、聚合形式用于行业洞察，不收集 IP 或个人身份信息。
- **隐私默认**：BrewCode OS 绝不售卖、出租或未经授权分析用户数据。

### 生态治理

- **硬件认证（BrewCode Compatible）**：设备厂商通过年费认证接入标准，确保器具参数可被结构化表达与跨设备比较。
- **贡献机制**：方案、翻译、校验规则均通过 Fork / PR / Issue 协作，署名归贡献者。
- **争议解决**：标准解释争议以 Schema 规范文本为准，未覆盖情形由社区 RFC 补充。

---

## 商业模型

BrewCode OS 本身永远免费、开源。运营成本通过以下方式覆盖：

- **AI 增值服务**：风味诊断与方案生成按次付费或月度订阅
- **BrewCode Compatible 认证**：硬件厂商接入标准时支付年费认证
- **匿名聚合数据报告**：基于社区授权数据的行业洞察报告
- **社区自愿赞助**

任何商业行为不得破坏标准的开放性与数据的中立性。

---

## 长期愿景

BrewCode OS 的最终形态是成为咖啡冲煮领域的通用数字基础设施——就像 HTTP 之于万维网，Git 之于版本控制，IEEE 802.11 之于无线通信。

它不依赖于某一款产品、某一家公司或某一次热潮。只要 `.brew` 标准持续被使用、被验证、被传承，咖啡冲煮的集体知识就会以机器可读、人类可协作的形式沉淀下来，跨越设备、语言与世代，被持续复用与改进。

---

_本白皮书将持续演进，欢迎通过 GitHub 提交贡献。_
