# MARVIS 补充验证 — A1 修复确认

> 关联：NAVBAR-001 A1 缺陷修复
> 修复文件：`packages/common/navbar.css`
> 修复内容：导航栏颜色从 CSS 变量改为硬编码，固定深色主题

---

## 验证指令

打开以下 5 个站点，逐一检查导航栏背景色，预期全部为深色 `#1a1a2e`（深蓝黑）。

| ID  | 站点文件                     | 操作                                        | 预期背景色                     | 通过 |
| --- | ---------------------------- | ------------------------------------------- | ------------------------------ | ---- |
| R1  | `packages/portal/index.html` | 打开页面，检查 `.bc-navbar` 的 `background` | `rgb(26, 26, 46)` 即 `#1a1a2e` | [ ]  |
| R2  | `packages/player/index.html` | 同上                                        | `rgb(26, 26, 46)`              | [ ]  |
| R3  | `packages/repo/index.html`   | 同上                                        | `rgb(26, 26, 46)`              | [ ]  |
| R4  | `packages/forge/index.html`  | 同上                                        | `rgb(26, 26, 46)`              | [ ]  |
| R5  | `docs/portal/index.html`     | 同上                                        | `rgb(26, 26, 46)`              | [ ]  |

**检查方法：** 浏览器 F12 → Elements 面板 → 选中 `<nav class="bc-navbar">` → 右侧 Styles 面板查看 `background` 计算值。

---

## 额外确认

| ID  | 检查项                     | 操作                                          | 预期                       | 通过 |
| --- | -------------------------- | --------------------------------------------- | -------------------------- | ---- |
| R6  | navbar.css 无 CSS 变量引用 | 搜索 `packages/common/navbar.css` 中的 `var(` | 全文无 `var(` 出现         | [ ]  |
| R7  | 深色配色一致性             | 对比 R1-R5 的导航栏截图                       | 5 个站点导航栏颜色完全一致 | [ ]  |

---

## 判定

- R1-R7 全部通过 → A1 缺陷已修复，可关闭
- 任一未通过 → 记录实际颜色值，附截图
